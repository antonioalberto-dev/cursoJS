const notas = [10, 4, 7, 3, 1, 98, 45];

/*
notas.push(9); //adiciona no final do array
notas.unshift(1); //adiciona no inicio do array
notas.pop(); //retira o ultimo elemento do array
notas.shift(); //retira do inicio do array
*/

console.log(notas instanceof Array); // verifica se é uma instancia de array

console.log(notas);

const raiz = n => n ** 0.5; // arrow function

console.log(raiz(9));